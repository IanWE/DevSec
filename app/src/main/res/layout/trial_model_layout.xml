<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_gravity="center">
    <TextView
        android:id="@+id/msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:layout_marginTop="8dp"
        android:text="    Thanks for granting &quot;Usage Stats&quot; permission. \n    Before we can start the user study, we need to test your phone hardware first.
        \n   Please begin your tests now by clicking the start button below."
        android:selectAllOnFocus="true"
        android:textSize="16sp" />

    <View android:layout_height="1px" android:background="#000000" android:layout_width="fill_parent"></View>

    <Button
        android:id="@+id/start"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="90dp"
        android:layout_marginTop="10dp"
        android:layout_marginRight="90dp"
        android:background="@drawable/button_shape"
        android:layout_below="@+id/layout_of_apps"
        android:text="Start"
        android:textAllCaps="false"
        android:textColor="#ff0"
        android:textSize="18sp" />
    <!--
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginTop="5dp"
        android:text="@string/Instruction"
        />
        -->
</LinearLayout>